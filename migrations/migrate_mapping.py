# we need to update path - so that is really path, not some made up ______
import glob

def main():
    # path is separated literally by '___'
    # in this case we need just to move folders to new locations
    sam_wc = '{prefix}/{df}/mapped/bwa__{params}/{path}/{seq_set_id}/{sample}/{preproc}/mapped.sam'

    # f



if __name__ == "__main__":
    main()